# Notebooks

- [`colab_bootstrap.ipynb`](colab_bootstrap.ipynb) — clone/pull, install, Drive mount, secrets. **Start here in Colab.**
- Add experiment notebooks beside it; import from `em_displacement_vlm` instead of pasting long training cells.
