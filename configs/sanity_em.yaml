run_name: sanity_em
seed: 42
model_id: null  # pass --model-id /path/to/FT_R32_gemma3_faces_seed42 or a Hub adapter id
base_model_id: unsloth/gemma-3-4b-it
base_model_revision: bf46152c47f5dd20b896357cb51abc4c03b8ee8c
dataset_id: idhantgulati/faces-vision-alignment
dataset_revision: e16884582fe756d79e5987237a30c685543cb0f6
n_samples: 50
n_responses: 3
generation_seed: 42
temperature: 0.7
top_p: 0.9
top_k: 50
repetition_penalty: 1.1
max_new_tokens: 512
do_sample: true
use_cache: true
# Canonical A100 sanity runs match the BF16 FT load. Set true only for a
# separately validated low-memory runtime.
load_in_4bit: false
use_heldout_split: true
split_name: extraction
split_root: null  # set to the same seed-specific frozen role used for FT
allow_legacy_unbound_adapter: false
use_wandb: false
wandb_project: em-displacement-vlm
wandb_entity: null
wandb_group: mft-gemma3-r32
# Candidate face sanity only. This held-out role avoids FT contamination, but
# it is still face-domain and cannot satisfy the OOD EM reproduction gate.
